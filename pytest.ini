[pytest]
asyncio_mode = strict
# asyncio's self-pipe uses loopback sockets on Windows — allow that interface
# so pytest-socket doesn't block event-loop setup for synchronous tests.
addopts = --allow-hosts=127.0.0.1,::1,localhost
